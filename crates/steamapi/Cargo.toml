[package]
name = "steamapi"
version = "0.1.0"
edition = "2024"
description = "Steam Web API client for game library and store queries"

[dependencies]
reqwest = { version = "0.13.2", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
