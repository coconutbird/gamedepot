[package]
name = "steamapi"
version = "0.1.0"
edition = "2024"
description = "Steam Web API client for game library and store queries"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
ureq = "3.2.0"
